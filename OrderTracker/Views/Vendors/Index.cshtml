@{
  Layout = "_Layout";
}

@using OrderTracker.Models;

<h1>Vendors</h1>

@if (@Model.Count == 0)
{
  <h3>No Vendors have been added</h3>
}
@foreach (Vendor vendor in Model)
{
  <h3>@vendor.Name</h3>
}